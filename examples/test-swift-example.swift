import UIKit

class ViewController: UIViewController {
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        let userName = "John"
        let userAge = 25
        let isActive = true
        
        // Test variables for log message generation
        let data = ["name": userName, "age": userAge]
        
        // This is where we would test the log message insertion
    }
    
    func processUserData() {
        let users = ["Alice", "Bob", "Charlie"]
        let count = users.count
        
        // Another place to test log insertion
    }
}
