// TypeScript example
interface User {
  name: string;
  age: number;
}

function calculateSum(a: number, b: number): number {
  const result = a + b;
  return result;
}

const user: User = {
  name: 'John',
  age: 30,
};

console.log('Hello from TypeScript!');
